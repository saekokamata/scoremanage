    <div>
      <%= current_user.child_name %>さんの成績
    </div>
    
  <!-- ↓タブメニュー -->
    <%= render 'tabs' %>
  <!-- ↑タブメニュー -->

        <div id="science">
          <%= render 'tests/test_subject',tests: @tests, subject: 'science' %>
          <%= paginate @tests %>
        </div>
